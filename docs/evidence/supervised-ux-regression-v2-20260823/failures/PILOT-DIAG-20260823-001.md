# PILOT-DIAG-20260823-001 — `provider_output_incomplete`

| 字段 | 值 |
| --- | --- |
| Status | `open` |
| Category | `provider_completion_integrity` |
| Campaign | `EXT-PILOT-15D41CA378E73503` |
| Source task | `V2-PUB-003` |
| Scenario | `clarification_required` |
| Candidate commitment | `7744770aa4a36c131476b95d6ed9be248cdefc3ab0f4f2a18d5111b85c9f0d11` |
| Deployment Git SHA | `fda5abfdafe1d7908af521b4595bd56bf2a796b3` |
| Stable outcome / reason | `controlled_failure / provider_output_incomplete` |
| Attempt terminal state | `excluded` after the participant used the technical-failure continuation path |
| Executor wall-clock elapsed | 约 `28.5 s`（精确值绑定在脱敏 aggregate projection；数据库字段 `provider_latency_ms`，包含多轮编排和本地 backend 执行；不是纯 Provider latency，也不是 SLA） |
| Executor model requests | `2` |
| Model-requested tool calls | `1` |
| Backend-executed tool calls | `1` |
| Safety incident | `none` |

## 已知事实

- 系统没有把空或不完整的 Provider completion 展示为可评价答案；该 assignment 不进入
  UX feedback 分母。
- Safe telemetry 完整持久化，model requests、requested tool calls 与 backend execution
  分开记录。
- 当前稳定 reason 可能来自空白 `final_output`，也可能来自
  `raw_responses[*].output[*].status=incomplete`；现有脱敏 telemetry 无法区分这两个分支。
- Provider 原始 response body、Agent 输出正文、参与者身份与 notes 均未为诊断读取或写入
  本记录。

## 不能得出的结论

- 不能把这一次失败归因为 prompt、模型能力、工具 backend 或输出 token 上限中的任一项；
- 不能把 `2 model requests / 1 requested tool / 1 backend execution` 称为规划准确或错误；
- 不能用该 public task 调整 prompt 后再次把它描述为未见评测。

## 通用诊断计划

1. 使用不含本题文本的合成 SDK fixtures，分别覆盖：空白 `final_output`、raw response
   output item 的 `status=incomplete`、其他 output-item 非完成状态和 output-limit metadata。
2. 评估在 vNext 中增加不含正文的安全 completion subreason，例如
   `final_output_missing` 与 `response_status_incomplete`；不得持久化 Provider body。
3. 验证每个 subreason 的 model/tool/backend 计数、原子失败写入、DLP 和 append-only
   telemetry digest。
4. 若修改 `src/researchops`，必须创建新 Eval v2 candidate commitment；旧 68/93 和本轮
   telemetry 不得自动迁移到新 candidate。
5. 未经明确预算，不在线重跑本题或其他付费任务；不得用本题继续调 prompt。

## Exit criteria

- 合成 fixture 能稳定复现并区分 completion failure 分支；
- safe subreason、计数与 event binding 有无网络回归测试；
- 原始输出、API Key、Provider body 和参与者数据仍不持久化；
- 任何核心源码变更均进入新 candidate/version 后，才考虑不同任务上的新验证。

[返回本轮脱敏证据](../README.md)
