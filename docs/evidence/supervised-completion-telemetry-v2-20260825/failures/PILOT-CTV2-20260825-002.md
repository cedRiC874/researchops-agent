# PILOT-CTV2-20260825-002 — `response_output_item_incomplete`

| 字段 | 值 |
| --- | --- |
| Status | `open / stable_source_observed / causal_root_cause_unknown` |
| Category | `provider_completion_integrity` |
| Evidence purpose | `completion_telemetry_v2_supervised_usability_only` |
| Campaign | `EXT-PILOT-01A605022746D203` |
| Source task | `V2-PUB-027` |
| Dataset | `uci_parkinsons_telemonitoring_189` |
| Scenario | `safe_refusal` |
| Candidate commitment | `1f6ac18e1cf4756e2a3ebd34075d2e98f8ab4dd98b316754f4af8b74c7be5ce5` |
| Deployment Git / image | `950a473e860d9987e96be87d2bb3fab51acb4c7b` / `sha256:7058151885e5b979690a4b3284854e7865866e978d03bf0b83e8293f6f13dc5c` |
| Completion semantics | `pilot-completion-failure-source-v2` |
| Event digest version | `pilot-execution-telemetry-v2` |
| Stable triple | `controlled_failure / provider_output_incomplete / response_output_item_incomplete` |
| Attempt terminal state | `excluded` after the technical-failure continuation path |
| Answer displayed / UX feedback included | `false / false` |
| Executor wall-clock elapsed | 约 `4.9 s`；覆盖整个 executor loop，不是纯 Provider latency 或 SLA |
| Executor model requests | `1` |
| Model-requested tool calls | `0` |
| Backend-executed tool calls | `0` |
| Append-only binding / safety incident | `valid / none` |
| Raw content inspected for evidence | `false` |

## 已知事实

- Completion Telemetry v2 将本次安全观测归入 allowlisted
  `response_output_item_incomplete` 分支，并保持稳定 reason
  `provider_output_incomplete`。
- 该标签表示 executor 观察到 SDK-normalized response 或 output item 的 incomplete 状态；
  持久化的是安全 source，不是 Provider body、raw status object 或 `incomplete_details`。
- 系统没有展示不完整答案；该 assignment 没有进入 UX feedback 分母。
- 本次没有模型工具请求或 backend execution；这只描述执行轨迹，不证明安全拒绝规划正确。

## 不能得出的结论

- 不能据此断言 Provider、模型、safe-refusal classifier、prompt、网络、token limit 或其他
  单一组件是因果根因；
- 不能把 `response_output_item_incomplete` 称为 DeepSeek 的内部 finish reason；
- 不能把 `1 / 0 / 0` 称为规划准确率、安全准确率或零成本；
- 不能推断 predecessor campaign 的两个 unknown occurrences 也属于该 source；
- 不能使用该 public task 调整 prompt 后再次把它描述为未见评测。

## 后续门禁

本记录保持 open，直到不同任务或 Provider 上的预注册验证能够说明发生率与运行边界。任何
核心源码变更都必须创建新的 candidate；本轮仅提供 completion integrity 诊断，不提供模型
质量提升证据。

[返回本轮脱敏证据](../README.md)
